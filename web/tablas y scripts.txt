create table dse_13;

create table USR (
ID INTEGER NOT NULL,
USR_TIPO INTEGER NOT NULL,
USR_NAME VARCHAR(30),
USR_PASS VARCHAR(10)
);

create table USR_TIPO(
ID_TIPO INTEGER NOT NULL,
USR_TIPO VARCHAR (20)
);

ALTER TABLE USR ADD CONSTRAINT PK_ID PRIMARY KEY (ID);
ALTER TABLE USR_TIPO ADD CONSTRAINT PK_ID_TIPO PRIMARY KEY (ID_TIPO);
ALTER TABLE USR ADD FOREIGN KEY (USR_TIPO) REFERENCES USR_TIPO(ID_TIPO);